dashboard:
  prefix: "dashboard"

kubernetes:
  addons:   "/etc/kubernetes/addons"
  config:   "/home/{{ ansible_user }}/.kube"
  network:  "/etc/kubernetes/network"

  CIDR: "10.244.0.0/16"

metallb:
  addresses: "<MetalLB IPv4 addresses range>"
